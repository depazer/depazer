import { getModuleResolver } from '@/getModuleResolver'
import { describe, test } from 'vitest'

describe('getModuleResolver', () => {
  test.todo('should return an environment object', async ({ expect }) => {})
})
